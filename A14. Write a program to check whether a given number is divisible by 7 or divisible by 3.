#include <stdio.h>
#include <conio.h>

int main()
{
    int x;
    printf("enter a number\n");
    scanf("%d",&x);
    if (x%7==0 || x%3==0)
        printf("divisible");
    else
        printf("not divisible");
        getch();

}
