#include <stdio.h>
#include <conio.h>

int main()
{
   int a,b;
    printf("enter two number\n");
    scanf("%d %d",&a,&b);
    if (a>b)
        printf("%d is greater",a);
    else if(a<b)
        printf("%d is greater",b);
    else
        printf("%d and %d both are equal",a,b);
    getch();
}
