#include <stdio.h>
#include <conio.h>

int main()
{
    int x;
    printf("enter a number\n");
    scanf("%d",&x);
    if (x%5)
        //every zero-false
        printf(" not divisible");
    //every non zero-true
    else
        printf("divisible");

    getch();
}
