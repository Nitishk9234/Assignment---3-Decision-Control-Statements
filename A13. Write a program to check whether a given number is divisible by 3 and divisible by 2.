#include <stdio.h>
#include <conio.h>

int main()
{
    int x;
    printf("enter a number\n");
    scanf("%d",&x);
    if (x%3==0 && x%2==0)
        printf("divisible");
    else
        printf("not divisible");
        getch();

}
