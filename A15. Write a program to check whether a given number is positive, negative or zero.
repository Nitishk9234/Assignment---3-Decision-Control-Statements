#include <stdio.h>
#include <conio.h>

int main()
{
    int x;
    printf("enter a number\n");
    scanf("%d",&x);
    if (x>0)
        printf("positive");
        else
            if (x<0)
            printf("negative");
        else
            printf("zero");

    getch();

}
